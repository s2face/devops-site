# ТЗ для devops-writer: Урок 9 — Введение в сети: IP, DNS, TCP/UDP

## Метаданные
| Поле | Значение |
|------|----------|
| **Уровень** | Level 1 — Beginner |
| **Номер урока** | 9 из 15 |
| **Тема** | Введение в сети: IP, DNS, TCP/UDP |
| **Хэштеги** | `#networking` `#dns` `#tcp` `#ip` `#devops` |
| **Объём** | 2000–2500 слов |

## Цель урока
Читатель получит базовое понимание сетевых протоколов и сможет диагностировать сетевые проблемы с помощью стандартных утилит Linux.

## Целевая аудитория
Начинающие DevOps без опыта сетевого администрирования.

## Пререквизиты
- Урок 3: Bash, Урок 2: Ubuntu Server

## Технический стек
- **Команды:** `ip`, `ping`, `traceroute`, `netstat`, `ss`, `nslookup`, `dig`, `curl`, `wget`, `tcpdump`
- **Файлы:** `/etc/hosts`, `/etc/resolv.conf`, `/etc/network/interfaces`

## Требования к контенту
- [ ] Модель OSI — только нужные уровни (3, 4, 7) простыми словами
- [ ] IP-адреса: IPv4, маски подсети, CIDR-нотация
- [ ] Приватные диапазоны: 10.x, 172.16.x, 192.168.x
- [ ] TCP vs UDP — когда что используется
- [ ] DNS: как работает разрешение имён, A/CNAME/MX/TXT записи
- [ ] Диагностика: `ping`, `traceroute`, `dig`, `ss -tlnp`
- [ ] Настройка сети в Ubuntu: `ip addr`, `ip route`
- [ ] Firewall: `ufw` — базовые правила

## Структура статьи

### H1: Сети для DevOps: IP, DNS, TCP/UDP — без воды
### H2: Модель OSI — только то, что нужно знать
### H2: IP-адреса и маски подсети
```
192.168.1.0/24 → 254 хоста
10.0.0.0/8     → 16 млн хостов
```
### H2: TCP vs UDP — чем отличаются
### H2: DNS — как интернет знает куда идти
```bash
dig google.com A
nslookup -type=MX gmail.com
```
### H2: Диагностика сетевых проблем
```bash
ping -c 4 8.8.8.8
traceroute google.com
ss -tlnp   # открытые порты
```
### H2: Настройка firewall с ufw
### H2: Шпаргалка: сетевые утилиты DevOps-инженера

## KPI качества
- Таблица: частые порты (22, 80, 443, 3306, 5432, 6379)
- Алгоритм диагностики «сеть не работает» по шагам
- Объяснение DNS-резолвинга по шагам с примером

## Антипаттерны
- ❌ Обзор всей модели OSI (только нужные уровни)
- ❌ Упоминание `ifconfig` как основной команды (устарело, нужно `ip`)
- ❌ Отключение firewall вместо настройки правил
