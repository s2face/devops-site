# ТЗ для devops-writer: Урок 8 — Основы работы с SSH

## Метаданные
| Поле | Значение |
|------|----------|
| **Уровень** | Level 1 — Beginner |
| **Номер урока** | 8 из 15 |
| **Тема** | Основы работы с SSH |
| **Хэштеги** | `#ssh` `#security` `#linux` `#remote` `#keys` |
| **Объём** | 1800–2200 слов |

## Цель урока
Читатель настроит SSH-доступ к серверу через ключевую аутентификацию, научится использовать SSH-конфиг и базовые возможности SSH-туннелирования.

## Целевая аудитория
Начинающие DevOps/сисадмины.

## Пререквизиты
- Урок 2: рабочий Ubuntu Server
- Урок 4: управление пользователями

## Технический стек
- **OpenSSH:** 8.x+
- **Клиент:** ssh, scp, sftp, ssh-keygen, ssh-copy-id
- **Конфиг:** `~/.ssh/config`, `/etc/ssh/sshd_config`

## Требования к контенту
- [ ] Как работает SSH: асимметричное шифрование простыми словами
- [ ] Генерация SSH-ключевой пары (ed25519)
- [ ] Копирование публичного ключа на сервер
- [ ] Отключение парольной аутентификации в `sshd_config`
- [ ] `~/.ssh/config` — удобное управление подключениями
- [ ] Передача файлов: `scp` и `sftp`
- [ ] SSH port forwarding: Local и Remote tunneling
- [ ] `ssh-agent` и `ssh-add`

## Структура статьи

### H1: SSH от А до Я: безопасный доступ к серверам
### H2: Как работает SSH под капотом
- Схема: клиент ↔ сервер, public/private key
### H2: Генерация ключевой пары
```bash
ssh-keygen -t ed25519 -C "devops@myserver"
ssh-copy-id -i ~/.ssh/id_ed25519.pub user@server-ip
```
### H2: Отключаем пароли — только ключи
```bash
# /etc/ssh/sshd_config
PasswordAuthentication no
PubkeyAuthentication yes
```
### H2: ~/.ssh/config — ваш менеджер подключений
```
Host myserver
  HostName 192.168.1.100
  User devops
  IdentityFile ~/.ssh/id_ed25519
```
### H2: Передача файлов через scp и sftp
### H2: SSH-туннели: проброс портов

## KPI качества
- Читатель настроил вход по ключу и отключил пароли
- Есть схема асимметричного шифрования
- Предупреждение о защите приватного ключа passphrase

## Антипаттерны
- ❌ Оставлять root-вход по SSH открытым
- ❌ Использовать RSA 1024 вместо ed25519
- ❌ Хранить приватный ключ без passphrase на shared-машинах
